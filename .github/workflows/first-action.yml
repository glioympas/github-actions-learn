name: First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-22.04
    steps: 
      - name: Print hi world
        run: echo "Matestering CI with Github Actions"
      - name: Print bye
        run: echo "Byeee!"
